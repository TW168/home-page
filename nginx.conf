server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    if ($http_host = "www.aitherios.cloud") {
        return 301 https://aitherios.cloud$request_uri;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
